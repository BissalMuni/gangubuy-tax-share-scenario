# Specification

## Project: share-scenario

지방세법령 개정 내용을 기반으로 세금정보 제공 유튜브 영상 시나리오를 제작·공유·협업하는 플랫폼.

## 사용자 역할

| 역할 | 설명 |
|------|------|
| **관리자** | 시나리오 import, 트리 구조 관리, 상태 변경, 상태 일괄 갱신, 전체 관리 |
| **제작자** | 시나리오 열람·수정, 콘티 업로드, 콘티 열람·평가 |

- 사이트 접근 시 비밀번호 입력 필요 (`SITE_PASSWORD`)
- 관리자 기능은 `/admin` 경로, 별도 비밀번호(`ADMIN_PASSWORD`)로 보호

## User Stories

### US-1: 트리 네비게이션으로 시나리오 탐색

> As a 제작자, I want to 트리 구조로 시나리오 목록을 탐색하고 싶다, So that 원하는 법령/세목의 시나리오를 빠르게 찾을 수 있다.

- 드롭다운으로 분류 기준(법령별, 납세자 유불리 등) 전환 가능
- 트리 노드를 펼치고 접을 수 있다
- 리프 노드 클릭 시 해당 시나리오 상세 페이지로 이동
- 각 시나리오의 현재 상태(draft/revision/production/complete)가 트리에 표시

### US-2: 시나리오 상세 조회

> As a 제작자, I want to 시나리오의 원본 PDF와 스크립트를 함께 볼 수 있다, So that 법령 개정 내용과 영상 시나리오를 대조할 수 있다.

- 3열 레이아웃: PDF 뷰어 | 나레이션 스크립트 | 장면 구성
- 시나리오 메타정보 표시 (제목, 법률근거, 시행일, 영상길이 등)
- 나레이션 스크립트(script.md) 표시
- 장면별 구성(scenario.json의 scenes) 표시

### US-3: 시나리오 웹 수정

> As a 제작자, I want to 웹에서 시나리오 내용을 수정할 수 있다, So that 별도 도구 없이 바로 개선할 수 있다.

- 스크립트(나레이션) 마크다운 편집기
- 장면별 구성 수정 (비주얼, 나레이션, 텍스트 오버레이, 시간)
- 장면 추가/삭제 가능
- 메타정보 수정 (제목, 법률근거, 시행일, 영상길이)
- 수정 이력 확인 가능 (필드별 old/new 값 추적)
- 저장 시 시나리오 상태 자동 갱신

### US-4: 콘티(Storyboard) 업로드 및 공유

> As a 제작자, I want to 시나리오별로 콘티를 업로드하고 다른 사람의 콘티를 볼 수 있다, So that 다양한 영상 제작 아이디어를 공유할 수 있다.

- 시나리오별로 여러 명이 각자 콘티 업로드 가능
- 지원 형식: 이미지(JPG, PNG), PDF
- 드래그앤드롭 업로드 + 업로더 이름·설명 입력
- 콘티 목록 갤러리 형태로 조회
- 콘티에 대한 간단한 평가 가능 (기록 저장 불필요, 휘발성)
- 콘티 업로드 시 시나리오 상태 자동 갱신 (→ production)

### US-5: 시나리오 상태 관리

> As a 관리자, I want to 각 시나리오의 제작 단계를 관리할 수 있다, So that 전체 영상 제작 진행 현황을 파악할 수 있다.

- **자동 상태 판별**: 데이터 기반으로 상태 자동 결정
  - `draft`: 내용 없음
  - `revision`: 스크립트 또는 장면 데이터 존재
  - `production`: 콘티 업로드됨
  - `complete`: 스크립트 + 장면 + 콘티 + PDF 모두 존재
- 대시보드에서 수동 상태 변경도 가능 (드롭다운)
- 대시보드에서 전체 시나리오의 상태별 현황 조회 (통계 카드)
- 상태별 필터링
- 관리자 페이지에서 전체 시나리오 상태 일괄 갱신 기능

### US-6: 시나리오 데이터 초기 Import

> As a 관리자, I want to revised 폴더의 데이터를 일괄 import할 수 있다, So that 기존 작업 결과를 플랫폼에 올릴 수 있다.

- toc/ 폴더의 PDF를 Supabase Storage에 업로드
- scenarios/ 폴더의 scenario.json + script.md를 DB에 저장
- 폴더 구조를 기반으로 초기 트리 자동 생성
- **Import 방식: GitHub Actions 자동화** — revised/ 폴더를 GitHub에 푸시하면 워크플로우가 Supabase에 자동 동기화

### US-7: 트리 분류 관리

> As a 관리자, I want to 새로운 분류 기준의 트리를 추가할 수 있다, So that 다양한 관점으로 시나리오를 재분류할 수 있다.

- 관리자 웹 UI에서 트리 생성, 노드 추가/삭제
- 기존 시나리오를 새로운 트리 노드에 매핑

### US-8: 사이트 접근 제어

> As a 관리자, I want to 비밀번호로 사이트 접근을 제한할 수 있다, So that 허가된 사람만 플랫폼을 이용할 수 있다.

- 비밀번호만 입력하는 단순 로그인 페이지 (회원가입 없음)
- 사이트 비밀번호는 `.env`의 `SITE_PASSWORD`에서 읽기
- 인증 시 7일간 유효한 쿠키 발급
- Next.js middleware로 전체 경로 보호 (로그인·인증 API 제외)

## 성공 기준

1. revised 폴더의 전체 시나리오(약 285개)가 import되어 트리에서 탐색 가능
2. 법령 기준 트리와 최소 1개 이상의 대체 분류 트리가 동시 제공
3. 시나리오 스크립트를 웹에서 수정하고 저장할 수 있음
4. 시나리오별 콘티를 업로드하고 갤러리로 조회 가능
5. 전체 시나리오의 제작 단계 현황을 대시보드에서 한눈에 파악 가능
6. 시나리오 상태가 데이터 변경에 따라 자동으로 전환됨
7. 사이트 접근이 비밀번호로 보호됨

## Clarifications

| # | 질문 | 결정 |
|---|------|------|
| 1 | 트리 편집 방식 | 관리자 웹 UI에서 편집 (트리 노드 추가/삭제, 시나리오 매핑) |
| 2 | 수정 권한 | 사이트 비밀번호 인증 후 누구나 수정 가능. 수정 이력은 DB에 저장 |
| 3 | 콘티 평가 방식 | 👍 좋아요 버튼 (클라이언트 상태, 새로고침 시 초기화) |
| 4 | 사이트 보호 | `SITE_PASSWORD`로 전체 사이트 보호, `/admin`은 별도 `ADMIN_PASSWORD` |
| 5 | Import 방식 | GitHub Actions 자동화 — revised/ 폴더를 GitHub에 푸시하면 Supabase 자동 동기화 |
| 6 | 상태 전환 | 자동 판별 (데이터 기반) + 대시보드에서 수동 변경 가능 |
